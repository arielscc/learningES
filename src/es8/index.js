//Funcion Object.entries
const data = {
  frontend: 'oscar',
  backend: 'Isabel',
  design: 'Ana'
}
const entries = Object.entries(data)
console.log(entries)

const data = {
  frontend: 'oscar',
  backend: 'Isabel',
  design: 'Ana'
}

const keys = Object.keys(data)
console.log(keys)

const data = {
  frontend: 'oscar',
  backend: 'Isabel',
  design: 'Ana'
}
const values = Object.values( data )
console.log(values)

const string = 'hello';
console.log(string.padStart(7, '_-') )
console.log(string.padEnd(10, '_-') )

